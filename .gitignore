# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Seguridad y depuración (auditoría 2026-07-20)
scratch/

# Paquetes de despliegue del auth-server. Se generan a demanda y PUEDEN
# CONTENER EL .env con la clave de firma de licencias: no entran al repositorio.
omni-auth-server-*.zip
fenixdev-auth*.zip

# Agente OmniDeploy: `agent.env` lleva la clave maestra que autoriza a
# registrar dispositivos, y `agent.json` el token de este equipo concreto.
# Ninguno de los dos entra al repositorio.
omnideploy-agent/agent.env
omnideploy-agent/agent.json
omnideploy-agent/CREDENCIALES-PARA-EL-CLIENTE.txt

*.mp3
history*.json
injection_result.json
omni_license.txt
*.key
ComfyUI/
piper/
__pycache__/
Guia_Licencias_Omni_Shield.html
Guia_Licencias_Omni_Shield.pdf
generate_pdf_guide.py

# Bases SQLite generadas al ejecutar o probar el servidor en local. Nunca deben
# viajar al repositorio ni al paquete de despliegue: pisarian las de produccion.
*.db
*.db-journal
*.db-wal
DESBLOQUEO
auth-server/public/downloads/*.exe
*.zip
*.exe
recursos-llama/
